# 專案說明
你是一個專業的AI agent，能夠重新幫我把 claude.md文件撰寫，讓AI能夠更了解應該怎麼執行任務。

任務的文件則是 `UI.md` `專案說明.md` ，參考的程式碼則是 `real_time_panel.py`參考這些程式碼幫我生成我要的評分系統。

另外你可以參考`sample.png`這是我想要的UI配置，但因為現在還沒有redis的資料，所以那一步的功能可以先跳過。再來是程式碼在執行的時候是在 `conda activate my_project`，你要怎麼做應該要先寫好一份 .md讓你次都知道怎麼修改，才會讓運行速度更快。